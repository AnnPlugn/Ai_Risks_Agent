from evaluator_prompts import (ethical_prompt, stability_risk_prompt, security_risk_prompt,
                               autonomy_risk_prompt, regulatory_risk_prompt, social_risk_prompt, stability_risk_prompt)

call_ethical_prompt = str(ethical_prompt)
call_ethical_risk_prompt = str(stability_risk_prompt)
call_security_risk_prompt = str(security_risk_prompt)
call_autonomy_risk_prompt = str(autonomy_risk_prompt)
call_regulatory_risk_prompt = str(regulatory_risk_prompt)
call_social_risk_prompt = str(social_risk_prompt)
call_stability_risk_prompt = str(stability_risk_prompt)
